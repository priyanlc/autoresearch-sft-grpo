adapter/
adapter_*/
__pycache__/
*.pyc
*.bak

# data/ holds competition source files (CC BY 4.0; see data/README.md).
# Ignore by default to keep run-derived artefacts (e.g., val_split.json
# regenerated by prepare.py, scratch CSVs) out of git, but negate the
# specific files the repo tracks intentionally.
data/*
!data/README.md
!data/train.csv
!data/test.csv
